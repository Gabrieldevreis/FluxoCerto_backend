version: '3.8'

services:
  postgres:
    image: postgres:15-alpine # Versão leve e estável do Postgres
    container_name: nest_postgres_db
    restart: always
    environment:
      POSTGRES_USER: nest_user
      POSTGRES_PASSWORD: nest_password
      POSTGRES_DB: nest_db
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: 